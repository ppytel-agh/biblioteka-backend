{% extends 'base.html.twig' %}

{% block title %}Hello ToolsController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    <a href="{{ path('digit_string', {length: 10}) }}">
        <button>ISBN 10</button>
    </a>
    <a href="{{ path('digit_string', {length: 13}) }}">
        <button>ISBN 13</button>
    </a>
    <a href="{{ path('numer_karty') }}">
        <button>Numer karty</button>
    </a>
    <a href="{{ path('numer_egzemplarza') }}">
        <button>Numer egzemplarza</button>
    </a>
</div>
{% endblock %}
